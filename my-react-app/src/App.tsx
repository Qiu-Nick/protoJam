import type React from "react";
import ProgressBar from "./component/progressBar/ProgressBar";

const App: React.FC = () => {
	return (
		<div>
			<ProgressBar />
		</div>
	);
};

export default App;
